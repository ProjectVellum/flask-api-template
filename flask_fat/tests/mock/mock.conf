VERSION = 1.0
HOST = '0.0.0.0'
PORT = 5555
#Timeoutout in secondes per each HTTP REQUEST to offshore APIs that this
#server makes per each endpoint.
TIMEOUT = 1
DEBUG = True
# Whether or not "version=X.Y" in "Accept" header will be checked on each request.
# True - check version;
# False - no version check;
VERSION_CHECK = False
# Set True if you want server to crash during the loading sequence when there is
# an error. Set False - it will warn but keep going withouth crashing.
# default = True.
PEDANTIC_INIT = True
TESTING = True

HTTP_HEADERS = {
    'Accept' : 'Content-Type: application/json; charset=utf-8; version=1.0',
    'Access-Control-Allow-Origin' : '*'
}